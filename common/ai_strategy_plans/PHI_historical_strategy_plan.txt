PHI_historical_plan = {
	name = "Philippines Historical Plan"
	desc = "Historical behavior for Philippines"

	allowed = {
		original_tag = PHI
		has_dlc = "No Compromise, No Surrender"
	}

	enable = {
		OR = {
			AND = {
				is_historical_focus_on = yes 
				has_game_rule = {
					rule = PHI_AI_BEHAVIOR
					option = DEFAULT
				}
			}
			has_country_flag = PHI_AI_RANDOM_HISTORICAL
			has_game_rule = {
				rule = PHI_ai_behavior
				option = HISTORICAL
			}
		}
	}

	abort = {
	}

	ai_national_focuses = {
		
		#Getting started as historical, then the focus AI weights will take over.

	}

	#Focuses never to take
	focus_factors = {
	}			

	research = {

	}

	ideas = {

	}

	# Keep small, as it is used as a factor for some things (such as research needs)
	# Recommended around 1.0. Useful for relation between plans
	weight = {
		factor = 1.0
		modifier = {
			factor = 1.0
		}
	}

}